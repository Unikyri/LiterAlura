// Desactivar prerender para evitar bucles de recarga
// Como se menciona en la documentación de SvelteKit para casos con adapter-node
export const prerender = false;

// Configuración para SPA mode si es necesario
export const ssr = true; 